"""Shared MCP tool annotations — every tool is read-only and auto-approvable."""

READONLY_ANNOTATIONS = {
    "readOnlyHint": True,
    "destructiveHint": False,
    "idempotentHint": True,
    "openWorldHint": False,
}
