# Pinned exactly: oauth_compat.TolerantGoogleProvider mirrors 2.14.7 internals.
fastmcp==2.14.7
supabase>=2.5.0
pydantic>=2.0
pydantic-settings>=2.0
httpx>=0.27
redis>=5.0,<6
uvicorn>=0.30
starlette>=0.37
# OpenTelemetry traces + metrics + logs → New Relic OTLP. Loaded only when
# NEW_RELIC_LICENSE_KEY is set (telemetry.setup_telemetry); the API is used for
# instrumentation and no-ops otherwise. The otlp-proto-http exporter package ships the
# trace, metric AND log exporters. system-metrics adds host/process gauges (CPU/mem/RSS)
# and is imported optionally — its absence degrades to app-metrics-only, not a failure.
opentelemetry-sdk>=1.27,<2
opentelemetry-exporter-otlp-proto-http>=1.27,<2
opentelemetry-instrumentation-system-metrics>=0.48b0
