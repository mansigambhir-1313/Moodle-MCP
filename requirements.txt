# Pinned exactly: oauth_compat.TolerantGoogleProvider mirrors 2.14.7 internals.
fastmcp==2.14.7
supabase>=2.5.0
pydantic>=2.0
pydantic-settings>=2.0
httpx>=0.27
redis>=5.0,<6
uvicorn>=0.30
starlette>=0.37
# OpenTelemetry tracing → New Relic OTLP. Loaded only when NEW_RELIC_LICENSE_KEY is set
# (telemetry.setup_telemetry); the API is used for instrumentation and no-ops otherwise.
opentelemetry-sdk>=1.27,<2
opentelemetry-exporter-otlp-proto-http>=1.27,<2
